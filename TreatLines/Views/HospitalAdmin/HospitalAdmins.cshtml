@model IEnumerable<TreatLines.Models.Tables.HospitalAdminModel>

<div class="container row p-0 m-0">
    <h3 class="text-info">Hospital admins</h3>
    <div class="border p-3">
        <table id="hospitalAdmins_admin" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone number</th>
                    <th>Register date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var h in Model)
                {
                    <tr>
                        <td>@h.FullName</td>
                        <td>@h.Email</td>
                        <td>@h.PhoneNumber</td>
                        <td>@h.RegistrationDate</td>
                        <td>
                            <a class="btn btn-secondary"
                               asp-controller="HospitalAdmin"
                               asp-action="BlockUnblockUser"
                               asp-route-id="@h.Id"
                               asp-route-userType="0">
                                @(h.Blocked == 1 ? "Unblock" : "Block")
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>